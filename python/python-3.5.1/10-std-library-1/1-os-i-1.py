import os
os.getcwd()      # Return the current working directory
os.chdir('/sdcard/coding/python/')   # Change current working directory
os.system('mkdir today')   # Run the command mkdir in the system shell
